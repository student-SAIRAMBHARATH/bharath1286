name: 
on: [push]
jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v3
      - name: print a message
        run: echo "Hello From GitHub Actions users"
